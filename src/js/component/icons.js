import fontawesome from '@fortawesome/fontawesome';

import faClock from '@fortawesome/fontawesome-free-regular/faCheck';

fontawesome.config = {
    autoReplaceSvg:'nest'
}
fontawesome.library.add(faClock);